import "./index.css";

const Button = ({ buttonText, reqType, setReqType }) => {
  return (
    <button
      className="button"
      onClick={}
    >
      {buttonText}
    </button>
  )
}

export default Button