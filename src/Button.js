import "./index.css";

const Button = ({ buttonText, reqType, setReqType }) => {
  return (
    <button
      className="button"
      
    >
      {buttonText}
    </button>
  )
}

export default Button