import "./index.css";
import ListItem from "./ListItem";

const Result = ({ items }) => {
  return (
    <ul className="result">
      {items.map(item => (
        
      ))}
    </ul>
  )
}

export default Result